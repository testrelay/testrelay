- name: repos
  definition:
    url: http://backend-alb-1108022267.eu-west-2.elb.amazonaws.com/graphql
    timeout_seconds: 60
    forward_client_headers: true
  comment: ""
  permissions:
  - role: user
    definition:
      schema: |-
        schema  { query: RootQuery }

        type Repo { full_name: String
          id: Int
        }

        type RootQuery { repos(business_id: Int): [Repo]
        }
